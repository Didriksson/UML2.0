package ConstantsAndEnums;

public enum Enums {

	OPENFILE_ENUM,
	SAVEFILE_ENUM,
	CLASS_ENUM,
	ASSOCIATION_ENUM,
	DIRECT_ASSOCIATION_ENUM,
	INHERITANCE_ENUM,
	DEPENDENCY_ENUM,
	REALIZATION_ENUM,
	AGGREGATION_ENUM,
	COMPOSITION_ENUM,
	DELETE_ENUM,
	UNDO_ENUM,
	REDO_ENUM
		
};
